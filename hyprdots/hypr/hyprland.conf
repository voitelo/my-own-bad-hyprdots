################################################
### AUTOGENERATED CONFIG / HEAVY EDITED BY ME ##
################################################

# Edit this config according to the wiki instructions:
# https://wiki.hypr.land/Configuring/
# --------------------------------------------------------------
# Note: Not all options are set here. See the wiki for full list.
# You can split the config into multiple files:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

monitor=eDP-1,1920x1080@60,0x0,1

#########################
### PROGRAM VARIABLES ###
#########################

$terminal = kitty
$fileManager = dolphin
$menu = walker
$menu2 = wofi --show drun

#################
### AUTOSTART ###
#################
# Launch necessary processes or favorite apps at startup

exec-once = hyprpm reload -n
exec-once = source $HOME/.config/cursor-theme.sh
exec-once = hypridle &
exec-once = swaybg -i $HOME/lightV.png               
exec-once = waybar                                    
exec-once = Scripts/battery-warning.sh &                       
exec-once = swaync &
exec-once = notify-send -t 3500 'Welcome to Hyprland🫡' 
exec-once = sudo swapon /dev/sda3

#####################################
###   ENVIRONMENT VARIABLES    ######
#####################################

# See https://wiki.hypr.land/Configuring/Environment-variables/

# Sourcing file for cursor theme
source = ~/.config/hypr/cursor.conf

###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Changes require a Hyprland restart and are not applied on-the-fly for security reasons

# Example permissions
# permission = /usr/(bin|local/bin)/grim, screencopy, allow
  permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
  permission = /usr/(bin|local/bin)/hyprpm, plugin, allow
  permission = /usr/(lib|/bin)/flameshot, screencopy, allow

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# set hyprland config error colours:

# ----------------------
# General Settings
# ----------------------
general {
    gaps_in = 5
    gaps_out = 12

    border_size = 2

    col.active_border = rgba(b8f00cee) rgba(3ee17cff) 45deg
    col.inactive_border = rgb(229,101,101)

    # Enable resizing windows by dragging borders/gaps
    resize_on_border = true

    # Enable at your own risk, see wiki for tearing info
    allow_tearing = false

    layout = dwindle
}

# ----------------------
# Window Decoration
# ----------------------
decoration {
    rounding = 13
    rounding_power = 4

    # Window opacity
    active_opacity = 1.0
    inactive_opacity = 0.925

    # Shadow settings
    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # Blur settings
    blur {
        enabled = true
        size = 3
        passes = 2
        vibrancy = 0.1696
    }
}

# ----------------------
# Animations
# ----------------------
animations {
    enabled = yes

    # Bezier easing presets
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    # Default animations
    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# ----------------------------
# Workspace Rules / Smart Gaps
# ----------------------------
# Uncomment and adjust to enable "no gaps when only one window"
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# ------------------------
# Layout-specific Settings
# ------------------------

  master {
    orientation = left
  }

# ----------------------
# Miscellaneous
# ----------------------
misc {
    force_default_wallpaper = 0   # Set to 0 or 1 to disable anime mascot wallpapers
    disable_hyprland_logo = true   # Disable random Hyprland logo / anime girl background
}

#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options = caps:escape
    kb_rules =
    follow_mouse = 1
    sensitivity = -0.92
}

# https://wiki.hypr.land/Configuring/Variables/#gestures
gestures {
    workspace_swipe_forever = true
}

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = 0
    disable_while_typing = false
}


# -----------------
# KEYBINDINGS     |
# -----------------

# -----------------
# Mod key
# -----------------
# $mainMod = SUPER   # Sets "Windows" key as main modifier

# -----------------
# App launches
# -----------------
bind = SUPER, Q, exec, $terminal
bind = SUPER, E, exec, $fileManager
bind = SUPER, R, exec, $menu
bind = SUPER, Z, exec, walker --modules emojis
bind = Alt, R, exec, $menu2
bind = SUPER, G, exec, zen-browser --no-sandbox --disable-gpu
bind = SUPER, A, exec, hyprlock
bind = SUPER, W, exec, pkill waybar && waybar &
bind = SUPER, S, exec, flameshot gui
bind = , INSERT, exec, flameshot gui
bind = SUPER, K, exec, java -jar /home/leg/Downloads/TLauncher.jar
bind = SUPER, V, exec, flatpak run org.vinegarhq.Sober
bind = SUPER, N, exec, pkill waybar
bind = SUPER, M, exec, waybar 
bind = CTRL SUPER Alt, M, exec, waybar --config /etc/xdg/waybar/spacing-2/waybar/config.jsonc --style /etc/xdg/waybar/spacing-2/waybar/style.css &
bind = SUPER SHIFT, M, exec, waybar --config /etc/xdg/waybar/airy/waybar/config.jsonc --style /etc/xdg/waybar/airy/waybar/style/style-dark.css &

# -----------------
# Website shortcuts
# -----------------
bind = Alt, C, exec, zen-browser chatgpt.com
bind = Alt, Y, exec, zen-browser youtube.com
bind = Alt, X, exec, zen-browser modrinth.com

# -----------------
# Custom scripts
# -----------------

bind = Alt, tab, exec, bash Scripts/alt-tab-switcher.sh
bind = SUPER, B, exec, ./Scripts/sensitivity.sh
bind = Alt, P, exec, Scripts/wallpaper-toggle.sh
bind = Alt, O, exec, /home/leg/Scripts/cursor-change-only.sh
bind = SUPER, D, exec, python ~/Custom-Apps/SpaceMNGR.py
bind = Alt, A, exec, python /home/leg/Exported-Macros/email.py
bind = Alt, D, exec, python /home/leg/Exported-Macros/password2.py
bind = Alt, S, exec, python /home/leg/Exported-Macros/password.py
bind = Alt Shift, N, exec, python /home/leg/Scripts/toggle_animations.sh
bind = Super SHIFT, E, exec, bash /home/leg/test.sh
bind = SUPER, T, exec, ./Scripts/power-menu.sh

# -----------------------------
# Custom walker dmenu scripts
# -----------------------------

bind = Alt, U, exec, ./Scripts/notes.sh
bind = Alt, I, exec, ./Scripts/walker-clipboard.sh
bind = CTRL, Y, exec, ./Scripts/walker-music.sh
bind = CTRL, X, exec, ./Scripts/walker-scripts.sh
bind = CTRL, A, exec, ./Scripts/walker-minimize.sh
bind = CTRL, S, exec, ./Scripts/walker-wallpaper.sh

# -----------------
# Window management
# -----------------
bind = SUPER, C, killactive
bind = Alt, F4, killactive
bind = SUPER, J, togglesplit,    
bind = SUPER, Space, togglefloating
bind = Alt, F, fullscreen
bind = SUPER, Left, movefocus, l
bind = SUPER, Right, movefocus, r
bind = SUPER, Up, movefocus, u
bind = SUPER, Down, movefocus, d

# Workspace movement
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7
bind = SUPER SHIFT, 8, movetoworkspace, 8
bind = SUPER SHIFT, 9, movetoworkspace, 9
bind = SUPER SHIFT, 0, movetoworkspace, 10

# Incremental floating window resize (improved visibility)
bind = CTRL Shift, Right, resizeactive, 20 0   # grow width
bind = CTRL Shift, Left,  resizeactive, -20 0  # shrink width
bind = CTRL Shift, Down,  resizeactive, 0 20   # grow height
bind = CTRL Shift, Up,    resizeactive, 0 -20  # shrink height

# --------------
# Miscellaneous
# --------------
# add stuff here later

# Switch workspaces with mainMod + [0-9]
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7
bind = SUPER, 8, workspace, 8
bind = SUPER, 9, workspace, 9
bind = SUPER, 0, workspace, 10

# -----------------
# Mouse bindings
# -----------------
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

# -----------------
# Laptop multimedia keys
# -----------------
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = , XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = , XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# -----------------
# Media playback (playerctl)
# -----------------
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##################################
### WINDOWS AND WORKSPACES ###
##################################

# Refer to:
# - Window rules: https://wiki.hypr.land/Configuring/Window-Rules/
# - Workspace rules: https://wiki.hypr.land/Configuring/Workspace-Rules/

# ----------------------
# Example window rule
# ----------------------
#  windowrule = float,class:^(kitty)$,title:^(kitty)$

# ----------------------
# Suppress maximize requests from apps
# Useful to prevent apps from automatically maximizing
  windowrule = suppressevent maximize, class:.*

# ----------------------
# Fix dragging issues with XWayland apps
  windowrule = nofocus, class:^$, title:^$, xwayland:1, floating:1, fullscreen:0, pinned:0

# ----------------------
# Launch certain apps in a hidden workspace
# Move terminal to a hidden workspace when launching a specific app

windowrulev2 = workspace special:hidden, class:^(kitty)$, title:^(.*LaunchingAppName.*)$

# Make the app invisible (but still running)
windowrulev2 = opacity 0, noanim, class:^(kitty)$, title:^(.*LaunchingAppName.*)$

# make network manager GUI float
windowrulev2 = float, title:^(.*Network Manager.*)$

